-@title="Tags…"
-@flourish="&#57346;"

This page isn't finished yet… sorry.

%ul#tags
  -tags do |tag|
    %span.tag
      %label{ :class => "rate_#{tag.rate}" }=tag.title
      %ul
        -tag.articles[0,3].each do |article|
          %li.article
            %a{ :href => article.url, :alt => article.title }= article.title
            .abstract= article.abstract

        -if tag.articles.length > 3
          …
          %br
          == #{tag.articles.length - 3 } more article(s) tagged with »#{tag.title}«

        .bottom
          %a( href="/tag/#{tag.title}.html" alt="tag #{tag.title}" )>all articles with this tag

